# Keep search and read as public tools

`okfh evidence` should become the default answer-retrieval workflow, but it should not replace `okfh search` or `okfh read`. Search and read remain public, deterministic debugging and continuation tools for agents and people who need to inspect candidate concepts or fetch a specific bounded wiki document.
