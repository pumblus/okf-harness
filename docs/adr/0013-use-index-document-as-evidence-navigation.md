# Use the index document as evidence navigation

Evidence planning should read the OKF bundle's `index.md` first as a navigation surface for discovering likely concept documents. The index document can guide retrieval, but it should not be treated as default evidence text for an answer unless the question is specifically about the bundle's structure or homepage.
