# Use shared agent skill templates

OKF Harness renders Claude Code and Codex skills from shared templates instead of maintaining divergent manual copies. Adapter-specific renderers may inject small discovery and invocation differences, but the workflow contracts stay shared so the Tier 1 agent experience does not drift.
