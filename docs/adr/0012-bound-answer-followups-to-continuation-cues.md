# Bound answer follow-ups to continuation cues

When an evidence brief is insufficient, an agent may perform one automatic follow-up read, but only along continuation cues returned by OKF Harness. After that bounded follow-up, the agent should answer with an explicit evidence limit or ask the person whether to broaden the task; it should not turn a normal answer workflow into free exploration.
