# Include provenance pointers in evidence

Evidence items should include available provenance pointers such as reference-document links, source IDs, citation issues, and source-manifest metadata that is already safe to expose. These pointers help agents cite the synthesized wiki trail without expanding raw source bodies into normal answer workflows.
