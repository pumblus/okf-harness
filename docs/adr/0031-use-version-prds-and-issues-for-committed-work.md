# Use version PRDs and issues for committed work

Committed version work should move from discussion into a version-level PRD, then into agent-ready GitHub issues. The roadmap stays for uncommitted future demand, ADRs keep durable decisions, and the repo should not grow separate implementation-plan documents for active release work.
