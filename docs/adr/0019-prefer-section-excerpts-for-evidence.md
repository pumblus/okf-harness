# Prefer section excerpts for evidence

Evidence briefs should prefer Markdown section excerpts over arbitrary keyword windows. Sections preserve heading context, stable section IDs, offsets, and continuation cues; bounded offset ranges are the fallback when a document has no useful headings or a section is too large for the selected evidence budget.
